# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js build output
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local env files — never commit secrets
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# editor
.vscode/
.idea/

# data/embeddings.json is intentionally NOT ignored — we commit it so production
# deploys (and demos) don't need to re-run embedding. Add data/embeddings-*.json
# variants if you want them too.
